{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "parent_node": {
      "type": "string",
      "description": "The ID of the parent node being branched from."
    },
    "proposed_slug": {
      "type": "string",
      "description": "A short descriptive name for this new approach/hypothesis."
    },
    "changed_axes": {
      "type": "string",
      "description": "A summary of what parameters or code approaches are being changed compared to the parent."
    },
    "duplication_justification": {
      "type": "string",
      "description": "Explanation of why this approach is novel and not a duplicate of past attempts in the registry."
    },
    "next_run_command": {
      "type": "string",
      "description": "The exact shell command to execute the proposed experiment (e.g. scripts/run_experiment.sh ...)"
    },
    "plan_notes": {
      "type": "string",
      "description": "Any additional reasoning or notes for the plan."
    }
  },
  "required": ["parent_node", "proposed_slug", "changed_axes", "duplication_justification", "next_run_command", "plan_notes"],
  "additionalProperties": false
}