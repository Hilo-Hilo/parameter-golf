{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "reflection_notes": {
      "type": "string",
      "description": "Overall summary of the attempt and its outcome."
    },
    "is_success": {
      "type": "boolean",
      "description": "True if the experiment was a success and improved the metric or provided clear value."
    },
    "recommended_action": {
      "type": "string",
      "enum": ["keep", "discard", "branch"],
      "description": "The recommended next step for the controller."
    },
    "failure_reason": {
      "type": "string",
      "description": "One-line root cause if is_success is false (e.g. 'GQA crash in SDPA fallback'). Empty string if success."
    }
  },
  "required": ["reflection_notes", "is_success", "recommended_action", "failure_reason"],
  "additionalProperties": false
}